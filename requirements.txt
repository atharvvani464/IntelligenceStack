# Core sandbox runtime
duckdb>=1.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
pyyaml
fastapi
uvicorn
streamlit
requests
Faker
mlflow>=2.14.0
pytest

# Reference-architecture dependencies (used by the DLT / Unity Catalog / Vector
# Search modules that target a real Databricks workspace). Not required to run
# the local sandbox, but declared so the reference code installs cleanly.
pyspark>=3.5.0
databricks-sdk
